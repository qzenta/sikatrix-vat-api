{
  "name": "sikatrix-vat-api",
  "version": "1.0.0",
  "description": "Sikatrix VAT 201 validation & filing-prep service with x402 integration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "build": "tsc && esbuild src/index.ts --bundle --platform=node --target=es2020 --outfile=dist/index.js"
  },
  "dependencies": {
    "decimal.js": "^10.4.3",
    "hono": "^4.0.0",
    "x402-hono": "^1.2.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "wrangler": "^3.26.0"
  }
}
